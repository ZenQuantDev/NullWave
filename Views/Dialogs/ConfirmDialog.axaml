<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="using:NullWave.Helpers.Localization"
        x:Class="NullWave.Views.ConfirmDialog"
        Title="{loc:Loc Dialog_Confirm_Title}"
        Width="340" Height="160"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Background="{StaticResource BrushBase}">

    <DockPanel Margin="20">
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,16,0,0">
            <Button Content="{loc:Loc Dialog_No}"
                    Click="OnNo"
                    Classes="secondary"
                    Padding="16,8"/>
            <Button Content="{loc:Loc Dialog_Yes}"
                    Click="OnYes"
                    Classes="danger"
                    Padding="16,8"/>
        </StackPanel>
        <TextBlock Name="MessageText"
                   Foreground="{StaticResource BrushTextPrimary}"
                   FontSize="{DynamicResource FontSizeBody}"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>
    </DockPanel>
</Window>